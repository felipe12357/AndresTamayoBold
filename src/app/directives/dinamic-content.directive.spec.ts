import { DinamicContentDirective } from './dinamic-content.directive';

describe('DinamicContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DinamicContentDirective();
    expect(directive).toBeTruthy();
  });
});
